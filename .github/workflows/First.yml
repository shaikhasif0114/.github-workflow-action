#This is workflow name

name: Hello workflow
on:

   workflow_dispatch:
    
  #push:
     #branch:
      #- main
     #paths: "**.html"
jobs:
  True:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to users
        run: echo "Hello Dosto"
      - name: say hello to username
        run: echo "This is $USER"
  details:
    runs-on: ubuntu-latest
    steps:
      - name: What is the version of python & docker
        run: echo "docker --version && python --version"
